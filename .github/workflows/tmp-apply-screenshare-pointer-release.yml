name: Temporary Apply ShareScreen Pointer Release

on:
  push:
    branches:
      - feature/fix-screenshare-full-frame-release
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/apply-screenshare-pointer-release-fix.mjs
      - name: Commit patch if needed
        run: |
          if git diff --quiet -- src/components/Board.jsx; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/Board.jsx
          git commit -m "fix: release transient ShareScreen drag on pointerup"
          git push origin HEAD:feature/fix-screenshare-full-frame-release
