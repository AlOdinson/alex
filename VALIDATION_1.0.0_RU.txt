Проверки Alex Board 1.0.0

Выполнено:
- 19 JS/JSX-файлов разобраны TypeScript transpileModule без синтаксических ошибок;
- проверены все локальные относительные импорты;
- подтверждено, что applyCanvasInputMode() не вызывает canvas.getObjects();
- подтверждено, что applyCanvasInputMode() не вызывает requestRenderAll();
- подтверждено синхронное восстановление режима в capture-обработчике Pencil до возврата pointerdown в Fabric;
- подтверждено сохранение Pencil и частичного ластика;
- подтверждено соблюдение canEdit и режима пипетки;
- подтверждено подавление оставшихся пальцев старого pinch до их touchend;
- подтверждена проверка fingerIds/generation для запоздалого touchend;
- подтверждено, что touchend/touchcancel после zoom не вызывают тяжёлую configureBrushAndMode();
- package.json обновлён до 1.0.0;
- новый SQL отсутствует и не требуется.

Сборка:
- npm install / Vite build в данной среде не выполнены: внутренний npm-реестр вернул 404 для @supabase/supabase-js@2.110.8;
- это ограничение среды проверки, а не найденная ошибка исходного кода.

Ограничение:
реальный порядок событий Safari, Apple Pencil и двух пальцев окончательно проверяется только на физическом iPad. Код адресно исправляет воспроизводимую гонку zoom → первый Pencil-down.
